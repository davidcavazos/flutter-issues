import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      title: 'TextField error',
      home: Scaffold(
        appBar: AppBar(title: Text('TextField error')),
        body: TextField(),
      ),
    ));
